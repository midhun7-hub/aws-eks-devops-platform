apiVersion: apps/v1
kind: Deployment

metadata:
  name: payment-service
  namespace: qa

spec:
  replicas: 2

  selector:
    matchLabels:
      app: payment

  template:
    metadata:
      labels:
        app: payment

    spec:
      containers:
      - name: payment
        image: nginx:latest

        ports:
        - containerPort: 80